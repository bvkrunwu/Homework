
---

# 🗂️ Домашнее задание: Виджет банковских операций клиента

Это виджет, который показывает несколько последних успешных банковских операций клиента

---

---

## 📦 Текущая структура проекта

Homework/  
├── data/  
│   ├── operations.json   
│   ├── transactions.csv   
│   └── transactions_excel.xlsx   
├── htmlcov/  
├── src/  
│   ├── __ init __.py   
│   ├── bank_operations.py  
│   ├── decorators.py  
│   ├── external_api.py  
│   ├── generators.py  
│   ├── mask.py  
│   ├── processing.py  
│   ├── read_transactions.py  
│   ├── utils.py   
│   └── widget.py  
├── tests/  
│   ├── test_bank_operations.py  
│   ├── test_decorators.py  
│   ├── test_external_api.py  
│   ├── test_generators.py  
│   ├── test_mask.py  
│   ├── test_processing.py  
│   ├── test_read_transactions.py  
│   ├── test_utils.py  
│   └── test_widget.py 
│  
├── .coverage  
├── .env  
├── .env.example  
├── .flake8  
├── .gitignore  
├── main.py  
├── poetry.lock  
├── pyproject.toml  
└── README.md

---
---

## ⚙️ Реализованные модули и функции

### 🚀 Модуль `main.py`

#### Функция `main`

**Описание:**  

### Общая информация
Модуль main является центральным компонентом программного продукта, предназначенного для удобной работы с банковскими транзакциями. Этот модуль обеспечивает управление всей логикой взаимодействия с пользователем и координирует выполнение ключевых функций, предоставляемых различными вспомогательными модулями.

⚙️ Основные возможности модуля  
- Предоставление удобного графического интерфейса для загрузки и анализа финансовых транзакций из трёх популярных форматов файлов: JSON, CSV и XLSX.
- Гибкий механизм фильтрации транзакций по различным критериям, таким как статус операции (EXECUTED, CANCELED, PENDING), валюта транзакции, наличие определённых слов в описании операции.
- Возможность сортировки транзакций по дате проведения (возрастание/убывание).
- Автоматическая маскировка конфиденциальных данных (номера карт и счетов) перед отображением пользователю.
Интерактивная подсказка пользователю при некорректном вводе данных, предотвращающая аварийное завершение программы.

**Пример использования:**  

После запуска программы вам будет предложено выбрать формат файла, из которого будут загружены данные. Вы можете выбирать между JSON, CSV и XLSX. После успешной загрузки данных вы сможете применить фильтры и сортировку по вашему усмотрению.

Пример последовательности действий пользователя:

    1. Выберите пункт меню: Получить информацию о транзакциях из JSON-файла.
    2. Задайте критерий фильтрации по статусу операции: EXECUTED.
    3. Решите отсортировать операции по дате по возрастанию.
    4. Просмотрите итоговый список транзакций.

### Модуль `bank_operations.py`

#### Функция `process_bank_search`

**Описание:**  
Осуществляет поиск среди банковских операций по заданной строке в описании транзакций.  
Регулярное выражение позволяет гибкий поиск с учетом регистра букв.  

**Параметры:**  
data: Список словарей, каждый из которых представляет отдельную банковскую операцию.  
Обязательное требование — наличие ключа "description" в каждом словаре.  
search: Строка для поиска в описании операций.

**Возвращаемое значение:**  
Список словарей, содержащий только те операции, в описании которых была найдена заданная строка.

**Пример использования:**  

```
operations = [{'description': 'Оплата услуг связи'}, 
{'description': 'Покупка продуктов питания'}]

filtered_ops = process_bank_search(operations, 'услуги')for op in filtered_ops:
print(op['description'])  # Выведет: Оплата услуг связи
```

#### Функция `process_bank_operations`

**Описание:**  
Анализирует список банковских операций и подсчитывает количество операций по заданным категориям.  
Категории ищутся непосредственно в тексте описания каждой операции.

**Параметры:**  
data: Список словарей с банковскими операциями.  
Каждый словарь должен иметь ключ "description".  
categories: Список строк-категорий, которые будут искаться в описаниях операций.

**Возвращаемое значение:**  
Словарь, где ключи представляют собой указанные категории, а значения показывают количество найденных операций по каждой категории.

**Пример использования:** 

```
transactions = [{'description': 'Оплата коммунальных услуг'},
{'description': 'Продукты питания'},
{'description': 'Коммунальные услуги'}]

category_stats = process_bank_operations(transactions, ['коммунальные', 'продукты'])print(category_stats)
  # Выведет: {'коммунальные': 2, 'продукты': 1}
```


### Модуль `decorators.py`

#### Декоратор `log`

**Описание:**  
Данный декоратор добавляет функциональность логирования к целевой функции.
При успешном выполнении функции в лог добавляется запись вида '{имя_функции} ok'.
При возникновении исключения в лог добавляется запись с информацией об ошибке и переданными аргументах функции.
Логирование производится либо в указанный файл (если задан параметр filename), либо в стандартный поток вывода (консоль).
Исключения, возникающие при выполнении декорированной функции, перехватываются, регистрируются, а затем повторно возбуждаются для дальнейшей обработки.

**Параметры:**   
filename (Optional[str]): Имя файла для сохранения логов.  
По умолчанию None.
Если задан, логи будут сохраняться в указанный файл. Если не задан, логи будут выводиться в консоль.

**Возвращаемое значение:**  
Callable[[Callable[..., Any]], Callable[..., Any]]: Декорированная функция.

**Пример использования:** 
``` 
@log(filename="mylog.txt")
def my_function(x, y):
    return x + y

my_function(1, 2)

Ожидаемый вывод в лог-файл 
mylog.txt
 при успешном выполнении:

my_function ok

Ожидаемый вывод при ошибке:

my_function error: тип ошибки. Inputs: (1, 2), {}

Где 
тип ошибки
 заменяется на текст ошибки.
```

### Модуль `external_api`  

#### Функция `convert_transaction_amount_to_rubles`  

**Описание:**  
Конвертирует сумму транзакции в рубли, если валюта отличается от рублей.  
Сначала проверяется валюта транзакции. Если она равна USD или EUR, производится запрос к внешнему API для конвертации суммы в рубли.  
Для остальных валют возвращается оригинальная сумма.

**Параметры:**  
transaction (dict[str, Any]): Словарь, содержащий информацию о транзакции.  
Ожидается наличие ключа 'operationAmount', содержащего вложенный словарь с ключами 'amount' и 'currency'.  

**Возвращаемое значение:**  
float | None: Сумма транзакции в рублях или None в случае ошибки.

**Пример использования:**  

```
# Образец транзакции
sample_transaction = {
    "operationAmount": {
        "amount": "100",
        "currency": {
            "code": "USD"
        }
    }
}

# Преобразование суммы транзакции в рубли
rubles_amount = convert_transaction_amount_to_rubles(sample_transaction)

# Проверка результата
if rubles_amount is not None:
    print(f"Сумма транзакции в рублях: {rubles_amount:.2f}")
else:
    print("Ошибка конвертации валюты.")
```

### Модуль `generators`  

#### Функция `filter_by_currency`

**Описание:**  
Фильтрует список транзакций по заданному коду валюты. Проверяет соответствие валюты транзакции переданному коду либо по цифровому коду ISO 4217, либо по названию валюты.

**Параметры:**  
transactions (list[dict]) — список словарей, каждый из которых содержит информацию о конкретной финансовой транзакции.  
currency_code (str) — строка, представляющая код валюты (например, "USD" или "Доллар США").

**Возвращаемое значение:**  
Iterable[dict] — итератор, выдающий только те транзакции, валюта которых соответствует указанному коду или наименованию.  

**Пример использования:**

```
usd_transactions = filter_by_currency(transactions, "USD")
for _ in range(2):
    print(next(usd_transactions))

>>> {
          "id": 939719570,
          "state": "EXECUTED",
          "date": "2018-06-30T02:08:58.425572",
          "operationAmount": {
              "amount": "9824.07",
              "currency": {
                  "name": "USD",
                  "code": "USD"
              }
          },
          "description": "Перевод организации",
          "from": "Счет 75106830613657916952",
          "to": "Счет 11776614605963066702"
      }
      {
              "id": 142264268,
              "state": "EXECUTED",
              "date": "2019-04-04T23:20:05.206878",
              "operationAmount": {
                  "amount": "79114.93",
                  "currency": {
                      "name": "USD",
                      "code": "USD"
                  }
              },
              "description": "Перевод со счета на счет",
              "from": "Счет 19708645243227258542",
              "to": "Счет 75651667383060284188"
       }
```


#### Функция `transaction_descriptions`

**Описание:**
Позволяет получать описания транзакций поочередно из списка транзакций. Используется как удобный генератор для пошаговой обработки больших объемов данных.

**Параметры:**  
transactions (list of dicts) — список словарей, каждый из которых содержит информацию о транзакции, включая обязательное поле 'description'.

**Возвращаемое значение:**  
Generator — генератор, который при каждом обращении выдает очередное описание транзакции (str).

**Пример использования:**

```
descriptions = transaction_descriptions(transactions)
for _ in range(5):
    print(next(descriptions))

>>> Перевод организации
    Перевод со счета на счет
    Перевод со счета на счет
    Перевод с карты на карту
    Перевод организации
```

#### Функция `card_number_generator`

**Описание:**
Генерирует уникальные номера банковских карт в указанном диапазоне, формируя их в стандартном банковском формате XXXX XXXX XXXX XXXX.

**Параметры:**  
start (int) — целое число, определяющее начало диапазона генерации номеров карт (включительно).  
stop (int) — целое число, определяющее конец диапазона генерации номеров карт (включительно).

**Исключения:**  
TypeError: возникает, если любой из аргументов не является целым числом.  
ValueError: выбрасывается, если диапазон неверный (начало меньше 1, конец больше 9999999999999999 или начало больше окончания).

**Возвращаемое значение:**  
Generator — генератор, выдающий при каждом обращении новый уникальный номер банковской карты в формате XXXX XXXX XXXX XXXX.

**Пример использования:**

```
for card_number in card_number_generator(1, 5):
    print(card_number)

>>> 0000 0000 0000 0001
    0000 0000 0000 0002
    0000 0000 0000 0003
    0000 0000 0000 0004
    0000 0000 0000 0005
```

### Модуль`masks`
  
#### Функция `get_mask_card_number`

**Описание:**
Безопасно обрабатывает банковские карты, сохраняя видимыми лишь первые шесть и последние четыре цифры номера. Остальная часть номера маскируется звёздочками в формате XXXX XX** **** XXXX.

**Параметры:**  
card_number (str) — строка, содержащая номер банковской карты. Поддерживаются различные разделители (пробелы, тире, подчёркивания).  

**Возвращаемое значение:**  
str — замаскированный номер карты в указанном формате или сообщение об ошибке, если количество цифр отличается от 16.  

**Пример использования:**

```
# Маскировка номера банковской карты
card_num = "1234-5678-9012-3456"
masked_card = mask_card_number(card_num)
print(masked_card)  # Выведет: 1234 56** **** 3456
```

#### Функция `get_mask_account`

**Описание:**
Позволяет скрыть номер банковского счёта, показывая только последние четыре цифры в формате **XXXX.

**Параметры:**  
account_number (str) — строка, содержащая номер банковского счёта. Возможны разные разделители (пробелы, тире, подчёркивания). 

**Возвращаемое значение:**  
str — замаскированный номер счёта указанного формата или сообщение об ошибке, если число цифр меньше четырёх.
Пример использования:

```
# Маскировка номера банковского счёта
acc_num = "123456789012"
masked_acc = mask_account_number(acc_num)
print(masked_acc)  # Выведет: **1234
```


### Модуль `processing`  

#### Функция `filter_by_state`

**Описание:**
Отбирает из списка транзакций только те записи, чей статус соответствует указанному параметру. Удобна для быстрого отбора завершённых операций.

**Параметры:**  
transactions (list[dict]) — список транзакций, представленных в виде словарей с обязательным полем "state".  
state (str) — искомый статус транзакции. По умолчанию используется "EXECUTED" (выполненная транзакция).  

**Возвращаемое значение:**  
list[dict] — список транзакций, прошедших фильтрацию по выбранному статусу.  

**Пример использования:**

```
# Фильтрация транзакций по статусу EXECUTED
tx_list = [
    {'id': 1, 'state': 'EXECUTED'},
    {'id': 2, 'state': 'CANCELED'}
]
filtered_tx = filter_by_state(tx_list)
print(filtered_tx)  # Выведет: [{'id': 1, 'state': 'EXECUTED'}]

```

#### Функция `sort_by_date`

**Описание:**
Сортирует список операций по дате, позволяя выбрать порядок сортировки (по убыванию или возрастанию). Полезна для просмотра истории операций в нужном порядке.

**Параметры:**  
operations (list[dict]) — список операций, представленных в виде словарей с обязательным полем "date".  
descending (bool) — направление сортировки. По умолчанию установлено True (по убыванию, новейшие операции вверху).  

**Возвращаемое значение:**  
list[dict] — отсортированный список операций.

**Пример использования:**  

```
# Сортировка операций по дате (по убыванию)
ops_list = [
    {'date': '2023-01-01', 'amount': 100},
    {'date': '2023-12-31', 'amount': 200}
]
sorted_ops = sort_by_date(ops_list)
print(sorted_ops)  # Выведет: [{'date': '2023-12-31'}, {'date': '2023-01-01'}]
```

Судя по предыдущим сообщениям, речь идет о создании описания функций read_csv_transactions и read_excel_transactions в формате Markdown для раздела README.md. Эти функции предназначены для чтения транзакционных данных из CSV и Excel-файлов соответственно, причем обе реализованы примерно одинаково: принимают необязательный параметр с путем к файлу, определяют стандартное местоположение файла, если путь не задан, читают данные с помощью pandas, обрабатывают возможные исключения и возвращают результат в виде списка словарей либо пустой список в случае ошибок. Примеры данных тоже были приведены, что помогает лучше представить структуру возвращаемых объектов.

Пользователь хочет получить именно описание этих функций в стиле предыдущего примера, где была показана структура для другой функции convert_transaction_amount_to_rubles. Значит, важно сохранить тот же подход: сначала общее описание каждой функции, затем перечислить параметры, подробно описать возвращаемые значения, привести пример использования и добавить пояснения по структуре данных. Для полноты картины стоит упомянуть, что оба метода используют pandas для парсинга файлов, а также отметить обработку исключительных ситуаций.

Учитывая всё это, я подготовлю два отдельных описания для read_csv_transactions и read_excel_transactions, следуя той же схеме, что и в примере, чтобы итоговый документ выглядел последовательно и понятно.

Вот детальное описание функций `read_csv_transactions` и `read_excel_transactions` в формате Markdown, подходящее для включения в файл `README.md`:

---

### Модуль `read_transactions`

Модуль предназначен для удобной загрузки финансовых транзакций из файлов формата CSV и Excel и последующего преобразования их в структуры Python (списки словарей) для дальнейшего анализа и обработки.

---

#### Функция  `read_csv_transactions`

**Описание:**  
Загружает транзакционные данные из CSV-файла и возвращает их в виде списка словарей. Автоматически выбирает стандартный путь к файлу, если таковой не указан явно. Использует библиотеку Pandas для эффективной обработки данных.

**Параметры:**  
**file_path_csv**: `Optional[str]`  
  Путь к CSV-файлу с транзакционными данными. Если не указан, используется стандартный путь: `<корень_проекта>/data/transactions.csv`.

**Возвращаемое значение:**  
`List[Dict[str, Any]]`: Список словарей, где каждый словарь представляет отдельную транзакцию. Структура каждого элемента включает ключевые поля, такие как `id`, `state`, `date`, `amount`, `currency_name`, `currency_code`, `from`, `to`, `description`.  
В случае ошибки чтения файла возвращается пустой список.

**Пример использования:**  

```python
# Чтение транзакций из стандартного CSV-файла
csv_transactions = read_csv_transactions()

# Отображение первых трех записей
print(csv_transactions[:3])
```

---

#### Функция  `read_excel_transactions`

**Описание:**  
Загружает транзакционные данные из Excel-файла и возвращает их в виде списка словарей. Подобно предыдущей функции, автоматически выбирает стандартный путь к файлу, если он не указан явно. Используется библиотека Pandas для эффективного взаимодействия с таблицами Excel.

**Параметры:**  
file_path_excel**: `Optional[str]`  
  Путь к Excel-файлу с транзакционными данными. Если не указан, используется стандартный путь: `<корень_проекта>/data/transactions_excel.xlsx`.

**Возвращаемое значение:**  
`List[Dict[str, Any]]`: Список словарей, где каждый словарь представляет отдельную транзакцию. Структура аналогична CSV-варианту и включает аналогичные поля: `id`, `state`, `date`, `amount`, `currency_name`, `currency_code`, `from`, `to`, `description`.  
В случае ошибки чтения файла возвращается пустой список.

**Пример использования:**  

```python
# Чтение транзакций из стандартного Excel-файла
excel_transactions = read_excel_transactions()

# Отображение первых трёх записей
print(excel_transactions[:3])
```

---


### Модуль `utils`

#### Функция `load_transactions`

**Описание:**   
Функция загружает финансовые транзакции из указанного JSON-файла.

**Параметры:**  
file_path (Optional[str | Path]): Путь к файлу JSON с финансовыми транзакциями.
По умолчанию используется файл 'operations.json' в папке 'data'.

**Возвращаемое значение:**  
Optional[List[Dict[str, Any]]]: Список словарей с данными транзакций, либо пустой список в случае ошибок или неправильного формата данных.

Пример использования:

```
transactions = load_transactions(None)
print(transactions)

>>> [{'id': 441945886, 'state': 'EXECUTED', 'date': '2019-08-26T10:50:58.294041', 'operationAmount': {'amount': '31957.58', 'currency': {'name': 'руб.', 'code': 'RUB'}}, 'description': 'Перевод организации', 'from': 'Maestro 1596837868705199', 'to': 'Счет 64686473678894779589'},
     {'id': 41428829, 'state': 'EXECUTED', 'date': '2019-07-03T18:35:29.512364', 'operationAmount': {'amount': '8221.37', 'currency': {'name': 'USD', 'code': 'USD'}}, 'description': 'Перевод организации', 'from': 'MasterCard 7158300734726758', 'to': 'Счет 35383033474447895560'}
]

```


### Модуль `widget`  

#### Функция `ask_account_card`

**Описание:**  
Автоматически распознаёт тип финансового инструмента (банковская карта или счёт) и применяет соответствующую маску к номеру. Упрощает процесс маскировки, устраняя необходимость ручного выбора нужной функции.

**Параметры:**  
info_string (str) — строка, содержащая название карты/счёта и его номер (разделяются пробелом).

**Возвращаемое значение:**  
str — замаскированная версия номера карты/счёта или сообщение об ошибке, если данные переданы некорректно.  

**Пример использования:**

```
# Автоматическая маскировка карты и счёта
info_str = "Сбербанк 123456******1234"
masked_info = mask_account_card(info_str)
print(masked_info)  # Выведет: Сбербанк 1234 56** **** 1234
```


#### Функция `get_date`

**Описание:**
Преобразует дату из формата YYYY-MM-DD в формат DD.MM.YYYY, обеспечивая удобную визуализацию временных меток.

**Параметры:**  
date_string (str) — строка даты в международном формате YYYY-MM-DD.

**Возвращаемое значение:**  
str — строка даты в формате DD.MM.YYYY.

**Пример использования:**

```
# Преобразование даты
date_str = "2023-12-31"
formatted_date = get_date(date_str)
print(formatted_date)  # Выведет: 31.12.2023
```



📝 **Примечание:** Данный раздел будет регулярно обновляться по мере реализации новых возможностей проекта.

------

## 🚧 Реализованная функциональность

На текущий момент проект находится в ранней стадии разработки. Функционал минимален и предназначен исключительно для демонстрации структуры и базовых принципов организации кода. По мере прохождения курса сюда будут добавлены конкретные модули и функции вместе с детальной документацией и примерами использования.

---
---

## 🧪 Тестирование
Проект использует фреймворк pytest для написания и проведения автоматизированных тестов. Для оценки качества покрытия кода тестами применяется инструмент coverage.py.

## Установка зависимостей
Перед запуском тестов убедитесь, что установлены необходимые пакеты:  

```
# Установка через Poetry
poetry add --group dev pytest
```

```
# Через poetry с добавлением в отдельную группу
poetry add --group dev pytest-cov
```

## Запуск зависимостей

```
pytest src/
```

```
pytest --cov 
```

```
#сгенерировать отчет о покрытии в HTML-формате
#Отчет будет сгенерирован в папке htmlcov и храниться в файле с названием index.html
pytest --cov=src --cov-report=html
```

---
---

## 📋 Логирование

В данном проекте реализовано детальное логирование для отслеживания процессов работы программы, обнаружения возможных проблем и упрощения процесса отладки. Ниже представлены ключевые моменты реализации и настройки логирования.

---

### 🛠️ Зачем использовано логирование?

Логирование позволяет отслеживать работу приложений, выявлять ошибки и анализировать поведение системы в процессе эксплуатации. Оно особенно полезно при разработке и поддержке сложных проектов, позволяя оперативно реагировать на возникающие проблемы.

---

### 🗂️ Модули с поддержкой логирования

На текущий момент логирование реализовано для следующих ключевых модулей нашего проекта:

- `utils.py`: вспомогательные утилиты общего назначения.
- `masks.py`: обработка и маскировка конфиденциальных данных (банковские карты, счета и др.)

---

### ⚙️ Настройки логирования

#### Базовая настройка логгера:

Используется встроенная библиотеку Python `logging` для удобной и гибкой конфигурации логирования. Вот основные шаги настройки:

1. **Создание экземпляра логгера**: Каждый модуль имеет собственный экземпляр логгера, созданный с использованием имени модуля.
   ```python
   logger = logging.getLogger("module_name")
   ```
   
2. **Установка уровня логирования**: Минимальный уровень логирования установлен на `DEBUG`, что позволяет регистрировать даже самые мелкие события.
   ```python
   logger.setLevel(logging.DEBUG)
   ```

3. **Хендлеры (обработчики)**:
   - Используется `FileHandler` для записи логов в файл.
     ```python
     file_handler = logging.FileHandler("logs/module_name.log", mode="w", encoding="utf-8")
     file_handler.setLevel(logging.DEBUG)
     ```
     
4. **Форматировщик (Formatter)**:
   - Записи логов включают метку времени, имя модуля, уровень важности и само сообщение.
     ```python
     formatter = logging.Formatter("{asctime} - {name} - {levelname}: {message}", style="{")
     file_handler.setFormatter(formatter)
     ```

5. **Подключение обработчиков к логгеру**:
   ```python
   logger.addHandler(file_handler)
   ```

---

### 🖥️ Примеры использования логирования в коде

Примеры включения логирования непосредственно в функциях модулей:

#### Пример из модуля `masks.py`:

```python
try:
    logger.info("Начало работы функции get_mask_card_number")
    ...
except Exception as e:
    logger.error(f"Произошла ошибка при обработке номера карты '{card_number}': {e}")
    raise
finally:
    logger.info("Завершение работы функции get_mask_card_number")
```

#### Пример из модуля `utils.py`:

```python
try:
    logger.info("Начало работы функции load_transactions")
    ...
except Exception as e:
    logger.error(f"Произошла ошибка при обработке файла транзакций: {e}")
    raise
finally:
    logger.info("Завершение работы функции load_transactions")
```

---

### 📜 Структура лог-файлов

Лог-файлы хранятся в отдельной директории `./logs/` и имеют следующий формат именования:

- `maks.log` — логи модуля масок.
- `utils.log` — логи служебных утилит.

Каждый новый запуск приложения перезаписывает существующий лог-файл, сохраняя только свежие данные.

---

### 💡 Типичный пример лог-записи:

```
2025-10-20 23:41:28,883 - masks - INFO: Начало работы функции get_mask_card_number
2025-10-20 23:41:28,884 - masks - INFO: Успешно замаскирован номер карты '1234 56** **** 9876'
2025-10-20 23:41:28,884 - masks - INFO: Завершение работы функции get_mask_card_number
```

---

### 🧑‍💻 Использование логов

Рекомендуется регулярно просматривать лог-файлы для диагностики поведения приложения, выявления потенциальных проблем и оптимизации производительности. Особенно полезны логи становятся при возникновении неожиданных ситуаций или ошибок, поскольку позволяют детально восстановить хронологию событий.

---
